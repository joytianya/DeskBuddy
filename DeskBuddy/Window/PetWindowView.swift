// DeskBuddy/Window/PetWindowView.swift
import SwiftUI

struct PetWindowView: View {
    var body: some View {
        Rectangle()
            .fill(Color.red)
            .frame(width: 32, height: 32)
    }
}
